// Neural network layer modules - contains implementations of various neural network layers
// This module organizes all concrete neural network layer implementations
// Connected to: src/nn/module.rs (Module trait), src/nn/parameter.rs (parameters), src/tensor.rs (tensor operations)
// Contains: Linear layers, convolutional layers, normalization layers, etc.

pub mod linear;